<Window x:Class="Easy_Minecraft_Serverr.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Easy_Minecraft_Serverr"
        mc:Ignorable="d"
        Title="Easy Minecraft Serverr" Height="640" Width="1020"
        Background="#0F1115"
        FontFamily="Segoe UI">

    <Window.Resources>

        <local:StatusToBrushConverter x:Key="StatusToBrushConverter"/>
        <local:StatusToLabelConverter x:Key="StatusToLabelConverter"/>

        <!-- ===== Palette ===== -->
        <SolidColorBrush x:Key="BgBase" Color="#0F1115"/>
        <SolidColorBrush x:Key="BgSidebar" Color="#161A20"/>
        <SolidColorBrush x:Key="BgCard" Color="#1E222A"/>
        <SolidColorBrush x:Key="BgCardHover" Color="#262B35"/>
        <SolidColorBrush x:Key="BorderSubtle" Color="#2A2F3A"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#ECEFF4"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#8B93A5"/>
        <SolidColorBrush x:Key="Accent" Color="#34D399"/>
        <SolidColorBrush x:Key="AccentHover" Color="#2FBF8F"/>
        <SolidColorBrush x:Key="Danger" Color="#F87171"/>
        <SolidColorBrush x:Key="DangerHover" Color="#EF5A5A"/>

        <!-- ===== Base text style ===== -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        </Style>

        <!-- ===== Buttons ===== -->
        <Style x:Key="BaseButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BgCard}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSubtle}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource BgCardHover}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.4"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button" BasedOn="{StaticResource BaseButton}"/>

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="{StaticResource Accent}"/>
            <Setter Property="Foreground" Value="#0F1115"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentHover}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="{StaticResource Danger}"/>
            <Setter Property="Foreground" Value="#0F1115"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource DangerHover}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ===== TextBox ===== -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource BgCard}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSubtle}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="CaretBrush" Value="{StaticResource Accent}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== ComboBoxItem (the rows inside the dropdown) ===== -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{StaticResource BgCard}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgCardHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource Accent}"/>
                                <Setter Property="Foreground" Value="#0F1115"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== ComboBox (fully templated so the dropdown popup is dark too) ===== -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource BgCard}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderSubtle}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="MainBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6"/>

                            <ToggleButton x:Name="ToggleBtn"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                          Focusable="False" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Rectangle Fill="Transparent"/>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <Grid IsHitTestVisible="False" Margin="{TemplateBinding Padding}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="18"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0"
                                                   Content="{TemplateBinding SelectionBoxItem}"
                                                   ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                   VerticalAlignment="Center"/>
                                <Path Grid.Column="1" Data="M0,0 L4,4 L8,0 Z"
                                      Fill="{StaticResource TextSecondary}"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>

                            <Popup x:Name="Popup" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom" AllowsTransparency="True" Focusable="False"
                                   PopupAnimation="Slide" MinWidth="{TemplateBinding ActualWidth}">
                                <Border Background="{StaticResource BgCard}"
                                        BorderBrush="{StaticResource BorderSubtle}"
                                        BorderThickness="1" CornerRadius="6" Margin="0,4,0,0">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.4"/>
                                    </Border.Effect>
                                    <ScrollViewer MaxHeight="260" SnapsToDevicePixels="True">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== Slider ===== -->
        <Style TargetType="Slider">
            <Setter Property="Foreground" Value="{StaticResource Accent}"/>
        </Style>

        <!-- ===== Sidebar server list item ===== -->
        <Style x:Key="ServerListItem" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="8,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="{StaticResource BgCard}" CornerRadius="8" Padding="10,8">
                            <Border.BorderBrush>
                                <SolidColorBrush Color="Transparent"/>
                            </Border.BorderBrush>
                            <Border.BorderThickness>1</Border.BorderThickness>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="8" Height="8" VerticalAlignment="Center" Margin="0,0,8,0"
                                             Fill="{Binding Status, Converter={StaticResource StatusToBrushConverter}}">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="{x:Static local:ServerStatus.Running}">
                                                        <DataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                                     From="1" To="0.35" Duration="0:0:1"
                                                                                     AutoReverse="True" RepeatBehavior="Forever"/>
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.EnterActions>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14"/>
                                </StackPanel>
                                <TextBlock Text="{Binding SoftwareVersionLabel}"
                                           Foreground="{StaticResource TextSecondary}"
                                           FontSize="11" Margin="16,2,0,0"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgCardHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource Accent}"/>
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgCardHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ===== TabControl / TabItem — underline style ===== -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="4,8"/>
            <Setter Property="Margin" Value="0,0,20,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="3"/>
                            </Grid.RowDefinitions>
                            <ContentPresenter x:Name="Content" ContentSource="Header"
                                              HorizontalAlignment="Left" Margin="{TemplateBinding Padding}"/>
                            <Border x:Name="Underline" Grid.Row="1" Height="3" CornerRadius="2"
                                    Background="{StaticResource Accent}" Opacity="0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Underline" Property="Opacity" Value="1"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ===== LEFT: server list ===== -->
        <Border Grid.Column="0" Background="{StaticResource BgSidebar}" CornerRadius="10">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Margin="12,14,12,6">
                    <TextBlock Text="EASY MINECRAFT SERVERR" FontSize="11" FontWeight="Bold"
                               Foreground="{StaticResource TextSecondary}" Margin="4,0,0,10"/>
                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="AddServerButton" Content="+ Add Server" Style="{StaticResource AccentButton}"
                                Click="AddServerButton_Click" Width="82"/>
                        <Button x:Name="RemoveServerButton" Content="Remove"
                                Click="RemoveServerButton_Click" RenderTransformOrigin="0.837,0.561" Width="58"/>
                    </StackPanel>
                </StackPanel>

                <ListBox x:Name="ServerListBox"
                         ItemsSource="{Binding Servers}"
                         SelectedItem="{Binding SelectedServer, Mode=TwoWay}"
                         ItemContainerStyle="{StaticResource ServerListItem}"
                         Background="Transparent"
                         BorderThickness="0"
                         Margin="0,4,0,8"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>
            </DockPanel>
        </Border>

        <!-- ===== RIGHT: selected server details ===== -->
        <Border Grid.Column="2" Background="{StaticResource BgSidebar}" CornerRadius="10" Padding="20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <TextBlock Text="{Binding SelectedServer.Name, FallbackValue='No server selected'}"
                               FontSize="22" FontWeight="Bold"/>
                    <Border Background="{StaticResource BgCard}" CornerRadius="12" Padding="10,3" Margin="12,4,0,0"
                            VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Margin="0,0,6,0" VerticalAlignment="Center"
                                     Fill="{Binding SelectedServer.Status, Converter={StaticResource StatusToBrushConverter}}"/>
                            <TextBlock Text="{Binding SelectedServer.Status, Converter={StaticResource StatusToLabelConverter}}"
                                       FontSize="11" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <Rectangle Grid.Row="1" Height="1" Fill="{StaticResource BorderSubtle}" Margin="0,14,0,14"/>

                <TabControl Grid.Row="2" x:Name="DetailTabs" IsEnabled="{Binding IsServerSelected}">
                    <TabItem Header="CONSOLE" Margin="0,1,3,-1">
                        <Grid Margin="0,16,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Background="#0B0D10" CornerRadius="8" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1">
                                <TextBox x:Name="ConsoleOutput"
                                         IsReadOnly="True"
                                         BorderThickness="0"
                                         Background="Transparent"
                                         FontFamily="Cascadia Mono, Consolas"
                                         FontSize="12"
                                         Foreground="#4ADE80"
                                         VerticalScrollBarVisibility="Auto"
                                         TextWrapping="Wrap"
                                         Padding="12"
                                         Text=""/>
                            </Border>

                            <Grid Grid.Row="1" Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="CommandInput" Grid.Column="0" KeyDown="CommandInput_KeyDown" Margin="0,0,85,0"/>
                                <Button x:Name="SendCommandButton" Content="Send"
                                        Click="SendCommandButton_Click" Margin="568,0,39,0"/>
                                <Button x:Name="StartStopButton" Content="Start" Margin="614,0,0,0"
                                        Style="{StaticResource AccentButton}" Click="StartStopButton_Click" Grid.ColumnSpan="3"/>
                            </Grid>
                        </Grid>
                    </TabItem>

                    <TabItem Header="SETTINGS" Margin="2,1,2,-1">
                        <StackPanel Margin="0,20,0,0" MaxWidth="360">
                            <TextBlock Text="Software" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <ComboBox x:Name="SoftwareCombo" HorizontalAlignment="Left" Width="220"
                                      SelectionChanged="SoftwareCombo_SelectionChanged"/>

                            <TextBlock Text="Minecraft Version" FontWeight="SemiBold" Margin="0,18,0,6"/>
                            <ComboBox x:Name="VersionCombo" HorizontalAlignment="Left" Width="220"
                                      SelectionChanged="VersionCombo_SelectionChanged"/>
                            <TextBlock x:Name="VersionStatusText" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,4,0,0"/>

                            <TextBlock Text="RAM Allocation" FontWeight="SemiBold" Margin="0,20,0,10"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Min" Foreground="{StaticResource TextSecondary}" FontSize="11" VerticalAlignment="Center" Width="30"/>
                                <Slider x:Name="RamMinSlider" Width="150" Minimum="512" Maximum="16384"
                                        TickFrequency="512" IsSnapToTickEnabled="True"
                                        ValueChanged="RamMinSlider_ValueChanged"/>
                                <TextBlock Text="{Binding ElementName=RamMinSlider, Path=Value, StringFormat={}{0:0} MB}"
                                           VerticalAlignment="Center" Width="70" Margin="8,0,0,0" FontFamily="Consolas"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="Max" Foreground="{StaticResource TextSecondary}" FontSize="11" VerticalAlignment="Center" Width="30"/>
                                <Slider x:Name="RamMaxSlider" Width="150" Minimum="512" Maximum="16384"
                                        TickFrequency="512" IsSnapToTickEnabled="True"
                                        ValueChanged="RamMaxSlider_ValueChanged"/>
                                <TextBlock Text="{Binding ElementName=RamMaxSlider, Path=Value, StringFormat={}{0:0} MB}"
                                           VerticalAlignment="Center" Width="70" Margin="8,0,0,0" FontFamily="Consolas"/>
                            </StackPanel>

                            <TextBlock Text="Port" FontWeight="SemiBold" Margin="0,20,0,6"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBox x:Name="PortBox" Width="100" FontFamily="Consolas"/>
                                <Button x:Name="SavePortButton" Content="Save" Margin="8,0,0,0" Click="SavePortButton_Click" Width="38"/>
                            </StackPanel>
                            <TextBlock x:Name="PortStatusText" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,6,0,0"/>
                        </StackPanel>
                    </TabItem>

                    <TabItem x:Name="PluginsModsTab" Header="PLUGINS/MODS" Margin="2,0,-1,0">
                        <Grid Margin="0,20,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock x:Name="AddonHeaderText" Grid.Row="0" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,10"/>

                            <Border Grid.Row="1" Background="{StaticResource BgCard}" CornerRadius="8" BorderBrush="{StaticResource BorderSubtle}" BorderThickness="1">
                                <ListBox x:Name="AddonListBox" FontFamily="Consolas" Background="Transparent" BorderThickness="0" Padding="6"/>
                            </Border>

                            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
                                <Button x:Name="AddAddonButton" Content="+ Add .jar" Style="{StaticResource AccentButton}" Click="AddAddonButton_Click" Width="63"/>
                                <Button x:Name="RemoveAddonButton" Content="Remove Selected" Style="{StaticResource DangerButton}" Margin="8,0,0,0" Click="RemoveAddonButton_Click" Width="105"/>
                                <Button x:Name="OpenAddonFolderButton" Content="Open Folder" Margin="8,0,0,0" Click="OpenAddonFolderButton_Click" Width="76"/>
                            </StackPanel>
                        </Grid>
                    </TabItem>
                </TabControl>
            </Grid>
        </Border>
    </Grid>
</Window>
